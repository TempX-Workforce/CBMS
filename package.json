{
    "name": "cbms",
    "version": "1.0.0",
    "description": "College Budget Management System - Monorepo",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
        "start": "node server/index.js",
        "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
        "install-all": "npm install && npm install --prefix client && npm install --prefix server",
        "local": "concurrently \"npm start --prefix client\" \"npm run dev --prefix server\""
    },
    "keywords": [
        "monorepo",
        "cbms"
    ],
    "author": "CBMS Team",
    "license": "MIT",
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "mongoose": "^9.0.1"
    }
}
